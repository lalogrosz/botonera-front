/*import { ButtonModel } from '../models/button.model';
import { Actions, ActionTypes } from './button.actions';

// Section 1
const initialState: ButtonModel[] = [];

// Section 2
export function reducer(state: ButtonModel[] = initialState, action: Actions) {

    // Section 3
    switch (action.type) {
        case ActionTypes.PLAY:
            const button: ButtonModel = action.payload;

            return [...state, action.payload];
        default:
            return state;
    }
}
*/
